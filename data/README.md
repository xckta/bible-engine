# Local data

Runtime-generated files live here and are ignored by Git:

- `bible.db` — local corpus/index database
- `local_settings.json` — local ESV API key/settings
- `sources/` — downloaded public-domain source archives

The repository intentionally does not store the ESV Bible corpus.
